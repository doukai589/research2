# Normalized Runs

New durable experiment runs should be written here in a standard layout:

```text
outputs/runs/<YYYYMMDD>_<short_name>/
  config.yaml
  metrics.csv
  summary.json
  report.md
  logs/
  checkpoints/
```

Historical outputs remain in their original locations for reproducibility.

