# Workbench

Temporary trial boxes live here.

Create a new trial with:

```bash
python scripts/manage_trial.py new <trial_name>
```

Successful trials should be promoted into `sas_core/`, `configs/experiments/`, or stable `scripts/`.
Failed trials should be summarized and archived.

