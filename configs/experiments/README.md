# Experiment Configs

Stable experiment definitions should live here.

The long-term goal is to run experiments by changing config files rather than adding a new script for every idea.

Example future configs:

- `steegformer_physionetmi_fullft.yaml`
- `steegformer_physionetmi_sascert.yaml`
- `cbramod_bcic2a_sascert.yaml`
- `labram_physionetmi_fullft.yaml`

